<script lang="ts">
  /**
   * Users management page.
   * 
   * Displays user list with filters, pagination, and CRUD operations.
   * Only accessible to super_admin and admin_viewer.
   */
  import UserList from "$lib/components/UserList.svelte";
</script>

<UserList />

