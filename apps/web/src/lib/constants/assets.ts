/**
 * Shared asset paths. Single source of truth for static assets used across the app.
 * Reused by: Vista Display zero-alert, timers store alerts, beep util, ServiceForm preview.
 */
export const ALERT_SOUND_SRC = "/sounds/alert.mp3";
