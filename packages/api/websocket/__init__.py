"""
WebSocket handlers for real-time updates.

This module handles WebSocket connections for timer updates and alerts.
Uses in-memory connection management (no Redis required for MVP).
"""
































